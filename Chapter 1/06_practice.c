#include<stdio.h>

    int main (){ 
    float r=10; // r is radius of circle
    printf("area of circle is %f \n", (3.14*r*r));

    int h = 10;// h is the height of cylinder
    printf("the volume of cylinder is %f\n",(3.14*r*r*h));

    return 0; 
}