# include<stdio.h>

int main(){
    // int a= 6, b, c; // this is type declaration instruction 
    // b=c=a;
    // printf("the value of a is %d\n",a);
    // printf("the value of b is %d\n",b);
    // printf("the value of c is %d\n",c);
    float a=8.9;
    float b= a+1.1;
    printf("the value of b is %f", b);
    return 0;
}