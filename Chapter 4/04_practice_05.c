#include<stdio.h>

    int main (){ 
    int i=0 ,n =10 , sum=0;
    while(i<=n){
        sum=sum+i;
        i++;
       
    }
    printf("the value of first ten natural no. is %d\n",sum );
    return 0; 
}