#include<stdio.h>

    int main (){ 
    int n;
    printf("enter the value of n \n");
    scanf("%d",&n);
    for(int a = 1 ; a<=n ; a++){
        printf("the first n natural no. is %d\n ",a);
    }
    
    return 0; 
}