#include<stdio.h>

    int main (){ 
    printf("hello \n");
    int a= 1;
    printf("%d\n",a);
    // if we print this 100 times than for short we use short command we use loop
    a++;
    printf("%d\n",a);
    return 0; 
}