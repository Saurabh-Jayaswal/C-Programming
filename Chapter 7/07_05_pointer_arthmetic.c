#include <stdio.h>

int main()
{
    // int i = 35;
    // int *ptr = &i ;
    // printf("the value of ptr is %u \n ", ptr);
    // // ptr++;
    // ptr--;
    // printf("the value of ptr is %u \n ", ptr);

    // char i = '35';
    // char *ptr = &i;
    // printf("the value of ptr is %u \n ", ptr);
    // // ptr++;
    // ptr--;
    // printf("the value of ptr is %u \n ", ptr);


     float i = 3.5;
    float *ptr = &i;
    printf("the value of ptr is %u \n ", ptr);
    // ptr++;
    ptr--;
    printf("the value of ptr is %u \n ", ptr);


    return 0;
}